[gcode_macro PID_BED]
description: Start Bed PID
gcode:
  G90
  {% if printer.toolhead.homed_axes != "xyz" %} G28 {% endif %}
  G1 Z10 F600
  M106 ; Turn on part cooling fan
  PID_CALIBRATE HEATER=heater_bed TARGET={params.TARGET_TEMP|default(70)|float}
  M107 ; Turn off fan
  SAVE_CONFIG